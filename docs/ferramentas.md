# Ferramentas

ECAC: DCTF WEB expõe 1 ferramenta (todas somente leitura).

### 1. `ecac_dctf_web_consultar`
**Input**: `pkcs12_cert`, `pkcs12_pass`, `data_inicial_apuracao` (opcional), `data_final_apuracao` (opcional), `data_inicial_transmissao` (opcional), `data_final_transmissao` (opcional), `perfil_procurador_cnpj` (opcional), `perfil_procurador_cpf` (opcional), `categoria_declaracao` (opcional), `situacoes_declaracao` (opcional), `numero_recibo` (opcional), `pagina` (opcional)

ECAC: DCTF WEB, consulta em fonte oficial.

## Prompts de exemplo

```
Consultar ECAC: DCTF WEB
```
